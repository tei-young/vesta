//
//  AppColors.swift
//  Vesta
//
//  Created on 2026-01-19.
//

import SwiftUI

struct AppColors {
    // MARK: - Primary Colors (핑크 계열)

    static let primary = Color(hex: "#FFA0B9")           // 메인 핑크
    static let primaryDark = Color(hex: "#F28AA5")       // 어두운 핑크
    static let primaryLight = Color(hex: "#FFCFDD")      // 연한 핑크

    // MARK: - Accent Colors (브라운 계열)

    static let accent = Color(hex: "#FBF9F7")            // 거의 화이트
    static let accentDark = Color(hex: "#7C5E4A")        // 다크 브라운
    static let accentLight = Color(hex: "#F5E6D3")       // 밀크 베이지
    static let accentHover = Color(hex: "#B89A7D")       // 호버 브라운

    // MARK: - Background Colors

    static let background = Color(hex: "#FEFAF7")        // 아이보리 배경
    static let card = Color.white                        // 화이트 카드
    static let highlight = Color(hex: "#F5E6D3")         // 밀크 베이지

    // MARK: - Text Colors

    static let textPrimary = Color(hex: "#2C2420")       // 브라운 블랙
    static let textSecondary = Color(hex: "#7C5E4A")     // 다크 브라운
    static let textTertiary = Color(hex: "#A0826D")      // 모카 브라운
    static let textAccent = Color(hex: "#FFA0B9")        // 핑크 강조

    // MARK: - Other Colors

    static let divider = Color(hex: "#E8D5C4")           // 베이지 구분선
}
